#!/bin/sh -x

case "${1}" in
   start)
      /usr/bin/loadkeys /etc/key.map
      ;;              

   *)                 
      echo "Usage:  ${0} {start}"
      exit 1          
      ;;              
esac
