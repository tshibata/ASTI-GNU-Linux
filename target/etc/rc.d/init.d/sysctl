#!/bin/sh
#!/bin/sh -x

case "${1}" in
   start)
      if [ -f "/etc/sysctl.conf" ]; then
         sysctl -p
      fi
      ;;

   status)
      sysctl -a
      ;;

   *)
      echo "Usage: ${0} {start|status}"
      exit 1
      ;;
esac
