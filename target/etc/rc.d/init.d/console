#!/bin/sh
#!/bin/sh -x

case "${1}" in
   start)
      # nouse for the moment
      ;;              

   *)                 
      echo "Usage:  ${0} {start}"
      exit 1          
      ;;              
esac
