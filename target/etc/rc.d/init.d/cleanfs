#!/bin/sh
#!/bin/sh -x

case "${1}" in
   start)
      rm -rf /tmp/*

      > /var/run/utmp
      chmod 664 /var/run/utmp
      chgrp utmp /var/run/utmp
      ;;

   *)
      echo "Usage: ${0} {start}"
      exit 1
      ;;
esac
