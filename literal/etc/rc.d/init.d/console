#!/bin/sh -x

case "${1}" in
   start)
      /bin/loadkeys /etc/key.map
      ;;              

   *)                 
      echo "Usage:  ${0} {start}"
      exit 1          
      ;;              
esac
